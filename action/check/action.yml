name: slim/check
description: Re-infer call sites and fail if a Slim envelope grew
runs:
  using: composite
  steps:
    - name: slim check
      run: node --experimental-strip-types "${{ github.action_path }}/../../src/github/check-action.ts"
      shell: bash
