name: slim/upstream
description: Query osv.dev for Slim replacements and fail if the slice is exposed
runs:
  using: composite
  steps:
    - name: slim upstream
      run: node --experimental-strip-types "${{ github.action_path }}/../../src/github/upstream-action.ts"
      shell: bash
      env:
        SLIM_UPSTREAM_PR: "1"
