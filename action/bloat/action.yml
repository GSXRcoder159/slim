name: slim/bloat
description: Fail PRs that add lodash/moment without a Slim replacement
runs:
  using: composite
  steps:
    - name: slim bloat
      run: node --experimental-strip-types "${{ github.action_path }}/../../src/github/bloat-action.ts"
      shell: bash
