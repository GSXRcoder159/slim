import type { Envelope } from "../envelope/types.ts";
export declare function assembleCatalogModule(env: Envelope, projectRoot?: string): string | null;
export declare function catalogFileFor(family: string, symbol: string): string;
