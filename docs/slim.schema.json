{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://unpkg.com/slim/slim.schema.json",
  "title": "slim.json",
  "description": "Optional. Zero-config default is src/slim + gitignore-aware scan.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "Optional. Absent means 1. Any other value is incompatible."
    },
    "outDir": {
      "type": "string",
      "default": "src/slim",
      "description": "Where generated slice modules and standing tests live."
    },
    "budgetMs": {
      "type": "integer",
      "minimum": 1,
      "description": "Extra generated fuzz cases after traces, literal unions, and timer taxonomy. Independent 5s case stall after worker ready. Default 30000, or 300000 if CI=1."
    },
    "testCommand": {
      "type": ["string", "null"],
      "default": null,
      "description": "Run after replace and during check. null = detect script 'test' in package.json, else skip."
    },
    "include": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Glob call-site roots. Default: whole repo minus gitignore and node_modules."
    },
    "ignore": {
      "type": "array",
      "items": { "type": "string" },
      "default": []
    },
    "replacements": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["version", "envelope", "module"],
        "properties": {
          "version": { "type": "string" },
          "envelope": { "type": "string" },
          "module": { "type": "string" }
        }
      },
      "description": "Recorded slices. Written by slim replace; slim check reads this."
    }
  }
}
