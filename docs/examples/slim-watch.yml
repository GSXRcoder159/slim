# Consumer example: weekly watch using the published upstream Action.
# This repository's dogfood workflow is .github/workflows/slim-upstream.yml
# (npm run build, then ./action/upstream).
name: slim-watch
on:
  schedule:
    - cron: "0 14 * * 1"
  workflow_dispatch:
jobs:
  watch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.18"
      - run: npm ci
      - uses: slim-hq/slim/action/upstream@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: slim-upstream-diagnostics
          if-no-files-found: ignore
          path: .slim/
